apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: bgd
spec:
  rules:
    - host: bgd.devnation
      http:
        paths:
          - path: /bgd
            pathType: Prefix
            backend:
              service:
                name: bgd
                port:
                  number: 8080
